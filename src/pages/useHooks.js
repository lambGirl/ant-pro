import React, { PureComponent } from 'react'

export default class UseHooksTest extends PureComponent{

    render = ()=>{
       /* const [count, setCount] = useState(0);
        return <div onClick={()=>{setCount(count+1)}}>
            试试useState
        </div>*/
       return <div>useHook的用法暂停</div>
    }
}

